apiVersion: 1

providers:
  - name: mt5bridge
    orgId: 1
    folder: MT5Bridge
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
